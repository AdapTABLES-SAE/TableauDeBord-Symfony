<div id="includedDetailsPartial" class="w-100 h-100 py-3">

    {# ================= TITLE AREA ================= #}
    <div class="section-title-area mb-1 text-center position-relative">

        <div class="position-absolute top-0" style="left: 20px;">
            <button
                id="duplicateTrainingBtn"
                class="btn btn-outline-primary"
                title="Dupliquer l'entraînement"
                data-action="{{ path('training_duplicate', {'id': training.id}) }}"
            >
                <i class="bi bi-files"></i>
            </button>
            <button
                id="deleteTrainingBtn"
                class="btn btn-outline-danger"
                data-action="{{ path('training_delete', {'id': training.id}) }}"
            >
                <i class="bi bi-trash"></i>
            </button>
        </div>

        <h2 class="fw-bold mb-3">
            Objectifs de l’entraînement
        </h2>

        <div class="input-group justify-content-center w-auto mx-auto">
            <input
                id="trainingTitle"
                name="name"
                type="text"
                class="save-able-field form-control text-center fw-semibold border-dark-subtle"
                value="{{ training.name }}"
            >
        </div>

    </div>

    {# ================= OBJECTIVES CAROUSEL ================= #}
    <div class="section-objectives-area mb-4 px-5 rounded-bottom-5">

        <div class="objectives-carousel" tabindex="0">

            <button
                class="carousel-arrow carousel-arrow-left"
                aria-label="Objectif précédent"
                type="button"
            >
                ‹
            </button>

            <div class="carousel-viewport">
                <div class="carousel-track mt-2 mb-2 pt-1 pb-1">

                    {% for objectif in objectifs %}
                        <article
                            class="carousel-slide objective-card"
                            data-id="{{ objectif.id }}"
                            data-name="{{ objectif.name }}"
                            data-edit-url="{{ path('objective_edit', {
                                entrainement: training.id,
                                id: objectif.id
                            }) }}"
                            tabindex="-1"
                        >
                            <div class="card-body d-flex flex-column position-relative">

                                <button
                                    type="button"
                                    class="objective-delete-btn"
                                    aria-label="Supprimer l’objectif"
                                >
                                    <i class="bi bi-trash"></i>
                                </button>

                                <i class="bi bi-chevron-right tile-chevron objective-chevron"></i>

                                <div class="objective-center-icon">
                                    <i class="bi bi-bullseye"></i>
                                </div>

                                <div class="objective-title text-truncate-multiline mb-3">
                                    {{ objectif.name }}
                                </div>

                                <div class="mt-auto text-center">
                                    <div class="tile-hint mb-1">
                                        Accéder au détail de l’objectif
                                    </div>

                                    <div class="objective-info">
                                        <i class="bi bi-layers me-1"></i>
                                        {{ objectif.niveaux|length }} niveau(x)
                                    </div>
                                </div>

                            </div>
                        </article>
                    {% endfor %}

                </div>
            </div>

            <button
                class="carousel-arrow carousel-arrow-right"
                aria-label="Objectif suivant"
                type="button"
            >
                ›
            </button>

            <div class="carousel-indicators" aria-hidden="true">
                {% for objectif in objectifs %}
                    <button
                        class="carousel-indicator"
                        type="button"
                        tabindex="-1"
                    ></button>
                {% endfor %}
            </div>
        </div>
    </div>

    {# ================= ACTION BAR ================= #}
    <div class="section-buttons-area d-flex justify-content-between align-items-center px-4">

        <div class="d-flex gap-2">
            <button
                id="input_cancel_button"
                class="btn btn-outline-secondary disabled"
            >
                Annuler les modifications
            </button>
        </div>

        <div class="d-flex gap-2">

            <form
                id="save_form"
                method="POST"
                action="{{ path('training_update', {'id': training.id}) }}"
                data-action="{{ path('training_update', {'id': training.id}) }}"
            >
                <button
                    type="submit"
                    id="input_save_button"
                    class="btn btn-outline-success disabled"
                >
                    Enregistrer
                </button>
            </form>

            <button
                id="addObjectiveBtn"
                class="btn btn-dark"
                data-action="{{ path('training_objective_add', {'id': training.id}) }}"
            >
                <i class="bi bi-plus-circle me-1"></i>
                Ajouter un objectif
            </button>

        </div>
    </div>

    {# ================= DELETE TRAINING MODAL ================= #}
    <div class="modal fade" id="deleteTrainingModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-danger">

                <div class="modal-header bg-light-subtle">
                    <h5 class="modal-title text-danger fw-bold">
                        Supprimer l’entraînement
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>

                <div class="modal-body">
                    Voulez-vous vraiment supprimer cet entraînement ?<br>
                    <strong>Cette action est irréversible.</strong>
                </div>

                <div class="modal-footer">
                    <button class="btn btn-secondary" data-bs-dismiss="modal">
                        Annuler
                    </button>
                    <button id="confirmDeleteTraining" class="btn btn-danger px-4">
                        Supprimer
                    </button>
                </div>

            </div>
        </div>
    </div>

    {# ================= CONFIRM SAVE TRAINING MODAL ================= #}
    <div class="modal fade" id="saveTrainingModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow">

                <div class="modal-header bg-light-subtle">
                    <h5 class="modal-title fw-bold">
                        Confirmer la modification
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>

                <div class="modal-body">
                    Voulez-vous enregistrer les modifications de cet entraînement ?
                </div>

                <div class="modal-footer">
                    <button class="btn btn-secondary" data-bs-dismiss="modal">
                        Annuler
                    </button>
                    <button id="confirmSaveTraining" class="btn btn-success">
                        Enregistrer
                    </button>
                </div>

            </div>
        </div>
    </div>

    {# ================= ADD OBJECTIVE MODAL ================= #}
    <div class="modal fade" id="addObjectiveModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">

                <div class="modal-header">
                    <h5 class="modal-title">Ajouter un objectif</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>

                <div class="modal-body">
                    <label for="objectiveNameInput" class="form-label">
                        Nom de l’objectif
                    </label>
                    <input
                        id="objectiveNameInput"
                        type="text"
                        class="form-control"
                        placeholder="Ex : Maîtriser les fractions"
                    >
                </div>

                <div class="modal-footer">
                    <button class="btn btn-secondary" data-bs-dismiss="modal">
                        Annuler
                    </button>
                    <button id="confirmAddObjective" class="btn btn-success">
                        Créer
                    </button>
                </div>
            </div>
        </div>
    </div>

    {# ================= DELETE OBJECTIVES SUMMARY MODAL ================= #}
    <div class="modal fade" id="deleteObjectivesSummaryModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-danger">

                <div class="modal-header bg-danger-subtle">
                    <h5 class="modal-title text-danger fw-bold mb-0">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        Suppression d’objectifs
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>

                <div class="modal-body text-start">
                    <p>Les objectifs suivants seront supprimés :</p>
                    <ul id="deleteObjectivesList" class="mb-0 ps-3"></ul>
                    <hr>
                    <p class="fw-bold text-danger mb-2">
                        ⚠ Cette action réinitialisera
                        <u>toutes les progressions de jeu</u>
                        associées à l’entraînement actuel.
                    </p>
                </div>

                <div class="modal-footer">
                    <button class="btn btn-secondary" data-bs-dismiss="modal">
                        Annuler
                    </button>
                    <button id="confirmDeleteObjectivesBtn" class="btn btn-danger px-4">
                        Supprimer
                    </button>
                </div>

            </div>
        </div>
    </div>
    {# ================= DUPLICATE TRAINING MODAL ================= #}
    <div class="modal fade" id="duplicateTrainingModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-primary">
                <div class="modal-header bg-light-subtle">
                    <h5 class="modal-title fw-bold text-primary">
                        <i class="bi bi-files me-2"></i>Dupliquer l'entraînement
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    Voulez-vous vraiment créer une copie de l'entraînement <strong>"{{ training.name }}"</strong> ?
                    <br><small class="text-muted">Tous les objectifs et niveaux seront copiés.</small>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                    <button id="confirmDuplicateBtn" class="btn btn-primary px-4">Confirmer la copie</button>
                </div>
            </div>
        </div>
    </div>
</div>
